import * as React from 'react'; import { clsx } from 'clsx'; export function Badge({className,...p}:React.HTMLAttributes<HTMLSpanElement>){return <span className={clsx('inline-flex items-center rounded-full border border-ink-200 px-2 py-0.5 text-xs font-medium text-ink-700',className)} {...p}/>}
